function add1(x, y) {
    return x + y;
  }
  
const add2 = (x, y) => {
return x + y;
};

const add3 = (x, y) => x + y;

const add4 = (x, y) => (x + y);

//add1 = add2 = add3 = add4

function not1(x) {
return !x;
}

const not2 = x => !x;

//not1 = not2

  